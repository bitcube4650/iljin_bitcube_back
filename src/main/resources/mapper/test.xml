<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">
  <select id="testUserId" resultType="camel">
    select USER_ID
    ,		USER_NAME
     from t_co_user tcu
     WHERE USER_ID = '050174' 
  </select>
  <select id="testUserIdCnt" resultType="int">
    select COUNT(*)
     from t_co_user tcu
     WHERE USER_ID = '050174' 
  </select>
</mapper>